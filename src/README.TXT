
* le fichier src/commun/fwk-bootstrap est un module contenant les déclarations et le paramètrage nécessaires à l'application.
 Il devra être importé par l'application principale au démarrage.

* Par défaut, le socle utilise les états (ui-router) suivants :
 ** 'error'
 ** 'login'
 ** 'unauthorized'
* C'est à l'application principale de les déclarer




 TODO
 template-common ?
 remettre dans common.js le controleur et l'état login ?
 voir comment paramétrer idpRestUrlBase et restBaseUrl ?
 emplacement des mocks ? Em realease, pas de mock ?
